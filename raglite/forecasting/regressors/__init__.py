"""Regressors package - external data fetchers by source.

This package contains individual modules for fetching regressors from different
data sources (ECB, ICE Futures, Eurostat, etc.).

The main fetch functions remain in raglite.forecasting.regressor_fetch for
backward compatibility.
"""
