# RAGLite CI/CD Pipeline
# Optimized with UV package manager for 10-100x faster installs
# Supports self-hosted macOS runners with parallel execution
# Based on Powerpoint Agent Generator CI best practices
# Updated: 2025-10-28 - Test Suite Consolidation
# Updated: 2025-10-29 - Runner Isolation (raglite label) + Resource Limits
#
# IMPORTANT: All tests consolidated in tests/ directory (2025-10-28)
# Total test count: ~402 tests (200+ unit, 115+ integration, 28 e2e)
# NFR validation: Accuracy (NFR6/NFR7), Performance (NFR13), Coverage (80%)
#
# RUNNER ISOLATION (2025-10-29):
# - Uses ONLY runners with 'raglite' label (raglite-runner-1, raglite-runner-2)
# - Prevents cross-project resource contention (20 total system runners)
# - Pytest workers limited to 4 (prevents 110-process explosion)
# - Expected: 2 jobs max parallel × 4 workers = 8 processes (~3-5 GB RAM)

name: CI

on:
  push:
    branches: [main, develop, "story/**", "epic/**"]
  pull_request:
    branches: [main, develop]
  workflow_dispatch:

# Restrict permissions (security best practice)
permissions:
  contents: read
  pull-requests: write
  checks: write

# Cancel in-progress runs for same branch/PR
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

# Global environment variables (inherited by ALL jobs)
env:
  APP_ENV: test
  CI: "true"

jobs:
  # ============================================================================
  # INFRASTRUCTURE VERIFICATION
  # ============================================================================

  # ============================================================================
  # JOB 0: Docker Daemon Health Check
  # Ensures Docker is available before running jobs that require it
  # ============================================================================
  docker-health-check:
    name: "🐳 Infrastructure: Docker Availability"
    runs-on: [self-hosted, raglite]
    timeout-minutes: 5
    outputs:
      docker-available: ${{ steps.check-docker.outputs.available }}

    steps:
      - name: Check Docker daemon availability
        id: check-docker
        run: |
          echo "========================================="
          echo "Docker Daemon Health Check"
          echo "========================================="

          # Check if Docker command exists
          if ! command -v docker &> /dev/null; then
            echo "❌ Docker command not found"
            echo "available=false" >> $GITHUB_OUTPUT
            exit 1
          fi

          # Check if Docker daemon is running
          if docker info &> /dev/null; then
            echo "✅ Docker daemon is running"
            docker version
            echo ""
            echo "Docker context:"
            docker context ls
            echo ""
            echo "available=true" >> $GITHUB_OUTPUT
          else
            echo "❌ Docker daemon is not running"
            echo ""
            echo "Docker context:"
            docker context ls || true
            echo ""
            echo "To fix this issue, start Docker Desktop:"
            echo "  open -a Docker"
            echo ""
            echo "available=false" >> $GITHUB_OUTPUT
            exit 1
          fi

      - name: Verify Docker socket
        if: steps.check-docker.outputs.available == 'true'
        run: |
          echo "Verifying Docker socket accessibility..."

          DOCKER_SOCKET=$(docker context inspect --format '{{.Endpoints.docker.Host}}' | sed 's|unix://||')
          echo "Docker socket path: $DOCKER_SOCKET"

          if [ -S "$DOCKER_SOCKET" ]; then
            echo "✅ Docker socket is accessible at $DOCKER_SOCKET"
          else
            echo "❌ Docker socket not found at $DOCKER_SOCKET"
            exit 1
          fi

  # ============================================================================
  # CODE QUALITY CHECKS
  # ============================================================================

  # ============================================================================
  # JOB 1: Linting & Formatting
  # ============================================================================
  lint:
    name: "📝 Code Quality: Lint & Format"
    runs-on: [self-hosted, raglite]
    timeout-minutes: 5

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install uv (network-resilient)
        run: |
          # Network-resilient uv installation with retry logic
          MAX_RETRIES=3
          RETRY_DELAY=2

          for attempt in $(seq 1 $MAX_RETRIES); do
            echo "Installing uv (attempt $attempt/$MAX_RETRIES)..."

            if curl --retry 3 --retry-delay 2 --retry-all-errors -LsSf https://astral.sh/uv/install.sh | sh; then
              echo "✅ uv installed successfully"

              # Verify installation
              if command -v uv &> /dev/null; then
                uv --version
                exit 0
              else
                echo "⚠️  uv command not found after installation"
              fi
            fi

            if [ $attempt -lt $MAX_RETRIES ]; then
              echo "⚠️  Installation attempt $attempt failed, retrying in ${RETRY_DELAY}s..."
              sleep $RETRY_DELAY
            fi
          done

          echo "❌ Failed to install uv after $MAX_RETRIES attempts"
          exit 1

      - name: Install dependencies with uv
        run: |
          uv venv ci-venv
          source ci-venv/bin/activate
          uv pip install ruff isort

      - name: Run Ruff linter
        run: |
          source ci-venv/bin/activate
          ruff check . --output-format=github
        continue-on-error: true

      - name: Run Ruff formatter check
        run: |
          source ci-venv/bin/activate
          # Use ruff format to match pre-commit hook configuration
          ruff format --check --diff raglite/ tests/ scripts/
        continue-on-error: true

      - name: Run isort import check
        run: |
          source ci-venv/bin/activate
          isort --check-only --diff raglite/ tests/ scripts/
        continue-on-error: true

  # ============================================================================
  # JOB 1.5: File Size Validation
  # ============================================================================
  file-size-check:
    name: "📏 Code Quality: File Size Limits"
    runs-on: [self-hosted, raglite]
    timeout-minutes: 2

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Check file sizes
        run: |
          echo "========================================="
          echo "FILE SIZE VALIDATION"
          echo "- Warning threshold: 400 LOC"
          echo "- Hard limit: 500 LOC"
          echo "- Mode: Block new violations in raglite/"
          echo "========================================="
          python3 scripts/check_file_sizes.py --verbose

  # ============================================================================
  # JOB 2: Type Checking
  # ============================================================================
  type-check:
    name: "🔍 Code Quality: Type Check (mypy)"
    runs-on: [self-hosted, raglite]
    timeout-minutes: 5

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install uv (network-resilient)
        run: |
          # Network-resilient uv installation with retry logic
          MAX_RETRIES=3
          RETRY_DELAY=2

          for attempt in $(seq 1 $MAX_RETRIES); do
            echo "Installing uv (attempt $attempt/$MAX_RETRIES)..."

            if curl --retry 3 --retry-delay 2 --retry-all-errors -LsSf https://astral.sh/uv/install.sh | sh; then
              echo "✅ uv installed successfully"

              # Verify installation
              if command -v uv &> /dev/null; then
                uv --version
                exit 0
              else
                echo "⚠️  uv command not found after installation"
              fi
            fi

            if [ $attempt -lt $MAX_RETRIES ]; then
              echo "⚠️  Installation attempt $attempt failed, retrying in ${RETRY_DELAY}s..."
              sleep $RETRY_DELAY
            fi
          done

          echo "❌ Failed to install uv after $MAX_RETRIES attempts"
          exit 1

      - name: Install dependencies with uv
        run: |
          uv venv ci-venv
          source ci-venv/bin/activate
          uv pip install -e .
          # SQLAlchemy 2.x has built-in type hints, no stubs needed (sqlalchemy-stubs is for 1.x only)
          uv pip install mypy types-requests types-setuptools

      - name: Run mypy on raglite/ package
        run: |
          source ci-venv/bin/activate
          mypy raglite/ --show-error-codes
        continue-on-error: false

  # ============================================================================
  # JOB 3: Security Scanning
  # ============================================================================
  security:
    name: "🔒 Code Quality: Security Scan"
    runs-on: [self-hosted, raglite]
    timeout-minutes: 5

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install uv (network-resilient)
        run: |
          # Network-resilient uv installation with retry logic
          MAX_RETRIES=3
          RETRY_DELAY=2

          for attempt in $(seq 1 $MAX_RETRIES); do
            echo "Installing uv (attempt $attempt/$MAX_RETRIES)..."

            if curl --retry 3 --retry-delay 2 --retry-all-errors -LsSf https://astral.sh/uv/install.sh | sh; then
              echo "✅ uv installed successfully"

              # Verify installation
              if command -v uv &> /dev/null; then
                uv --version
                exit 0
              else
                echo "⚠️  uv command not found after installation"
              fi
            fi

            if [ $attempt -lt $MAX_RETRIES ]; then
              echo "⚠️  Installation attempt $attempt failed, retrying in ${RETRY_DELAY}s..."
              sleep $RETRY_DELAY
            fi
          done

          echo "❌ Failed to install uv after $MAX_RETRIES attempts"
          exit 1

      - name: Install security scanning tools with uv
        run: |
          uv venv ci-venv
          source ci-venv/bin/activate
          uv pip install bandit[toml] safety
          uv pip install -e .

      - name: Run Bandit security linter
        run: |
          source ci-venv/bin/activate
          bandit -r raglite/ -f json -o bandit-report.json || true
        continue-on-error: true

      - name: Run Safety dependency scanner
        run: |
          source ci-venv/bin/activate
          safety check --json || true
        continue-on-error: true

      - name: Upload Bandit report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: bandit-security-report
          path: bandit-report.json
          retention-days: 30

  # ============================================================================
  # TEST SUITES
  # ============================================================================

  # ============================================================================
  # JOB 4: Unit Tests (~200 tests)
  # ============================================================================
  test-unit:
    name: "🧪 Tests: Unit (~200 tests)"
    runs-on: [self-hosted, raglite]
    timeout-minutes: 10

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install uv (network-resilient)
        run: |
          # Network-resilient uv installation with retry logic
          MAX_RETRIES=3
          RETRY_DELAY=2

          for attempt in $(seq 1 $MAX_RETRIES); do
            echo "Installing uv (attempt $attempt/$MAX_RETRIES)..."

            if curl --retry 3 --retry-delay 2 --retry-all-errors -LsSf https://astral.sh/uv/install.sh | sh; then
              echo "✅ uv installed successfully"

              # Verify installation
              if command -v uv &> /dev/null; then
                uv --version
                exit 0
              else
                echo "⚠️  uv command not found after installation"
              fi
            fi

            if [ $attempt -lt $MAX_RETRIES ]; then
              echo "⚠️  Installation attempt $attempt failed, retrying in ${RETRY_DELAY}s..."
              sleep $RETRY_DELAY
            fi
          done

          echo "❌ Failed to install uv after $MAX_RETRIES attempts"
          exit 1

      - name: Install dependencies with uv
        run: |
          uv venv ci-venv
          source ci-venv/bin/activate
          uv pip install -e .
          # FIX: Install pytest-mock (was missing) and pin versions to match local environment
          uv pip install \
            pytest==8.4.2 \
            pytest-cov==5.0.0 \
            pytest-asyncio==1.2.0 \
            "pytest-xdist>=3.5,<4.0" \
            "pytest-timeout>=2.0,<3.0" \
            pytest-mock>=3.15

      - name: Run unit tests with coverage
        run: |
          source ci-venv/bin/activate

          # FIX: Add project root and scripts to PYTHONPATH
          # This matches local Test Explorer environment where scripts/ is importable
          export PYTHONPATH="${PWD}:${PWD}/scripts:${PYTHONPATH}"

          echo "========================================="
          echo "CI MODE: Unit Tests with Coverage"
          echo "- Running ALL unit tests (including slow)"
          echo "- Parallel workers: 4 (safe parallelism, no shared state)"
          echo "- Target: <10 minutes"
          echo "- PYTHONPATH: ${PYTHONPATH}"
          echo "========================================="
          pytest tests/unit/ \
            -n 4 \
            --dist loadfile \
            -m "" \
            --cov=raglite \
            --cov-report=xml \
            --cov-report=term-missing \
            --cov-report=html \
            --junitxml=pytest-unit-report.xml \
            -v

      - name: Enforce coverage threshold (80%)
        run: |
          source ci-venv/bin/activate
          coverage report --fail-under=80
        continue-on-error: true

      - name: Upload unit test coverage
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage-unit
          path: |
            coverage.xml
            htmlcov/
            pytest-unit-report.xml
          retention-days: 30

  # ============================================================================
  # JOB 5: Integration Tests (~115 tests, requires Qdrant)
  # CI MODE: Uses 160-page full PDF for comprehensive testing
  # ============================================================================
  test-integration:
    name: "🔗 Tests: Integration (~115 tests)"
    runs-on: [self-hosted, raglite]
    timeout-minutes: 90  # Sequential execution requires 45-50 min + buffer for slow tests
    needs: [docker-health-check, test-unit]
    # CRITICAL FIX: Set APP_ENV=test at job level for consistent test database routing
    # This ensures ALL steps (health check, init, tests) use the same test infrastructure:
    # - Qdrant: port 6335, collection financial_docs_ci
    # - PostgreSQL: port 5433, database raglite_ci
    env:
      APP_ENV: test
      QDRANT_HOST: localhost
      QDRANT_PORT: 6335
      POSTGRES_HOST: localhost
      POSTGRES_PORT: 5433
      POSTGRES_DB: raglite_ci
      POSTGRES_USER: raglite_ci
      POSTGRES_PASSWORD: raglite_ci
      TEST_USE_FULL_PDF: "false"
      CI: "true"

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Pre-test memory cleanup (OOM prevention)
        run: |
          echo "========================================="
          echo "Pre-Test Memory Cleanup (Self-Hosted Runner)"
          echo "========================================="

          # Stop and remove any stale containers from previous runs
          echo "Cleaning up stale containers..."
          docker ps -a --filter "name=raglite-" --format '{{.Names}}' | xargs -r docker rm -f 2>/dev/null || true

          # Prune Docker resources to free memory
          echo "Pruning unused Docker resources..."
          docker system prune -f --volumes 2>/dev/null || true

          # Clear pip/uv caches if they're consuming too much memory
          # These can grow over time on self-hosted runners
          if [ -d ~/.cache/uv ]; then
            CACHE_SIZE=$(du -sm ~/.cache/uv 2>/dev/null | cut -f1 || echo "0")
            if [ "$CACHE_SIZE" -gt 2048 ]; then
              echo "Pruning large uv cache (${CACHE_SIZE}MB > 2GB limit)..."
              rm -rf ~/.cache/uv/builds-v0 2>/dev/null || true
            fi
          fi

          # Report initial memory state
          echo ""
          echo "Initial memory state:"
          if command -v vm_stat &> /dev/null; then
            PAGE_SIZE=$(sysctl -n hw.pagesize 2>/dev/null || echo 4096)
            FREE_PAGES=$(vm_stat | grep "Pages free" | awk '{print $3}' | tr -d '.')
            INACTIVE_PAGES=$(vm_stat | grep "Pages inactive" | awk '{print $3}' | tr -d '.')
            FREE_MB=$((FREE_PAGES * PAGE_SIZE / 1024 / 1024))
            INACTIVE_MB=$((INACTIVE_PAGES * PAGE_SIZE / 1024 / 1024))
            AVAILABLE_MB=$((FREE_MB + INACTIVE_MB))
            echo "  Available: ${AVAILABLE_MB}MB"
          fi

          echo ""
          echo "✅ Pre-test cleanup complete"

      - name: Install uv (network-resilient)
        run: |
          # Network-resilient uv installation with retry logic
          MAX_RETRIES=3
          RETRY_DELAY=2

          for attempt in $(seq 1 $MAX_RETRIES); do
            echo "Installing uv (attempt $attempt/$MAX_RETRIES)..."

            if curl --retry 3 --retry-delay 2 --retry-all-errors -LsSf https://astral.sh/uv/install.sh | sh; then
              echo "✅ uv installed successfully"

              # Verify installation
              if command -v uv &> /dev/null; then
                uv --version
                exit 0
              else
                echo "⚠️  uv command not found after installation"
              fi
            fi

            if [ $attempt -lt $MAX_RETRIES ]; then
              echo "⚠️  Installation attempt $attempt failed, retrying in ${RETRY_DELAY}s..."
              sleep $RETRY_DELAY
            fi
          done

          echo "❌ Failed to install uv after $MAX_RETRIES attempts"
          exit 1

      - name: Install dependencies with uv
        run: |
          uv venv ci-venv
          source ci-venv/bin/activate
          uv pip install -e .
          uv pip install \
            pytest==8.4.2 \
            pytest-asyncio==1.2.0 \
            "pytest-xdist>=3.5,<4.0" \
            "pytest-timeout>=2.0,<3.0" \
            pytest-mock>=3.15

      - name: Start and verify Qdrant (Test Instance)
        run: |
          # CI FIX: Use docker-compose test services for consistency and isolation
          CONTAINER_PORT="${QDRANT_PORT:-6335}"
          QDRANT_URL="http://localhost:${CONTAINER_PORT}"

          echo "Ensuring Qdrant TEST container on port ${CONTAINER_PORT}..."

          # AGGRESSIVE CLEANUP: Clear ALL Docker Compose state for qdrant-test
          # Root cause: Persistent Compose state survives across workflow runs on self-hosted runners
          echo "Performing aggressive Docker Compose state cleanup for Qdrant..."

          # Step 1: Try to stop service gracefully (may fail if state is corrupted)
          docker compose stop qdrant-test 2>/dev/null || true

          # Step 2: Remove service with force and orphan cleanup
          docker compose rm --force --stop qdrant-test 2>/dev/null || true

          # Step 3: Down with all flags to clear all state
          docker compose down --remove-orphans --volumes qdrant-test 2>/dev/null || true

          # Step 4: Direct container removal as fallback
          docker ps -a --filter "name=raglite-qdrant-test" --format '{{.Names}}' | xargs -r docker rm -f 2>/dev/null || true
          docker ps -a --format '{{.Names}} {{.Ports}}' | grep ":${CONTAINER_PORT}->" | awk '{print $1}' | xargs -r docker rm -f 2>/dev/null || true

          # Step 5: CRITICAL - Clear any stale container references from Docker's state
          # This removes containers that Compose thinks exist but don't
          docker container prune -f 2>/dev/null || true

          # Step 6: Clear any dangling volumes that might hold stale references
          docker volume prune -f 2>/dev/null || true

          # Step 7: macOS-specific - kill processes holding the port
          if command -v lsof > /dev/null 2>&1; then
            lsof -ti:${CONTAINER_PORT} | xargs -r kill -9 2>/dev/null || true
          fi

          # Step 8: Wait for Docker networking to fully release
          sleep 5

          # ENHANCED STORAGE HEALTH CHECK: More corruption detection
          # Root cause: cancel-in-progress can leave storage in bad state
          STORAGE_PATH="./qdrant_storage_test"
          if [ -d "$STORAGE_PATH" ]; then
            echo "Checking Qdrant storage health..."
            NEEDS_CLEANUP=false
            [ -f "$STORAGE_PATH/.lock" ] && NEEDS_CLEANUP=true && echo "⚠️  Stale lock file"
            find "$STORAGE_PATH" -name "*.tmp" -type f 2>/dev/null | grep -q . && NEEDS_CLEANUP=true && echo "⚠️  Temp files"
            [ ! -f "$STORAGE_PATH/raft_state.json" ] && [ ! -d "$STORAGE_PATH/collections" ] && NEEDS_CLEANUP=true && echo "⚠️  Missing files"
            [ ! -w "$STORAGE_PATH" ] && NEEDS_CLEANUP=true && echo "⚠️  Permission issue"
            [ "$NEEDS_CLEANUP" = true ] && rm -rf "$STORAGE_PATH" && echo "Removed corrupted storage"
          fi
          mkdir -p "$STORAGE_PATH" && chmod 755 "$STORAGE_PATH"

          # Start test services using docker-compose for consistency
          docker compose up -d qdrant-test

          # Verify container started
          if ! docker ps --filter "name=raglite-qdrant-test" --format '{{.Names}}' | grep -q "raglite-qdrant-test"; then
            echo "❌ Container failed to start!"
            docker logs raglite-qdrant-test --tail 50 || true
            exit 1
          fi

          # ENHANCED HEALTH CHECK: Longer timeout with diagnostics
          MAX_RETRIES=60
          RETRY_COUNT=0

          echo "Waiting for Qdrant TEST instance to be ready..."
          while [ $RETRY_COUNT -lt $MAX_RETRIES ]; do
            # Verify container still running
            if ! docker ps --filter "name=raglite-qdrant-test" --format '{{.Names}}' | grep -q "raglite-qdrant-test"; then
              echo "❌ Container stopped unexpectedly!"
              docker logs raglite-qdrant-test --tail 50 || true
              exit 1
            fi
            if curl -sf ${QDRANT_URL}/healthz > /dev/null 2>&1; then
              echo "✅ Qdrant TEST instance available at ${QDRANT_URL}"

              # Additional health check: verify cluster status
              CLUSTER_STATUS=$(curl -sf ${QDRANT_URL}/cluster | python3 -c "import sys, json; print(json.load(sys.stdin)['status'])" 2>/dev/null || echo "unknown")
              echo "Cluster status: $CLUSTER_STATUS"

              # Check telemetry for any recent 500 errors
              ERROR_500_COUNT=$(curl -sf ${QDRANT_URL}/telemetry 2>/dev/null | python3 -c "import sys, json; data=json.load(sys.stdin); print(sum(endpoint.get('500', {}).get('count', 0) for endpoint in data['result']['requests']['rest']['responses'].values()))" 2>/dev/null || echo "0")
              echo "Recent 500 errors: $ERROR_500_COUNT"

              if [ "$ERROR_500_COUNT" -gt 0 ]; then
                echo "⚠️  Warning: Qdrant has $ERROR_500_COUNT recent 500 errors"
                echo "Recreating Qdrant TEST container with fresh storage..."
                # AGGRESSIVE CLEANUP: Full state clear to prevent persistent corruption
                # Root cause: bind mount storage corruption from interrupted operations
                docker compose stop qdrant-test 2>/dev/null || true
                docker compose rm --force --stop qdrant-test 2>/dev/null || true
                docker compose down --remove-orphans --volumes qdrant-test 2>/dev/null || true
                docker container prune -f 2>/dev/null || true
                docker volume prune -f 2>/dev/null || true
                rm -rf ./qdrant_storage_test
                docker compose up -d qdrant-test
                sleep 5
                echo "✅ Qdrant TEST container recreated with fresh storage"
              fi

              exit 0
            fi

            RETRY_COUNT=$((RETRY_COUNT + 1))
            if [ $((RETRY_COUNT % 5)) -eq 0 ]; then
              echo "Waiting for Qdrant TEST... (attempt $RETRY_COUNT/$MAX_RETRIES)"
            fi
            sleep 1
          done

          echo "❌ Qdrant TEST instance not available after ${MAX_RETRIES}s"
          echo "Container status:"
          docker ps -a --filter "name=raglite-qdrant-test" --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"
          echo "Container logs:"
          docker logs raglite-qdrant-test --tail 50 || true
          echo "Storage contents:"
          ls -la "$STORAGE_PATH" 2>/dev/null || echo "Storage not accessible"
          exit 1

      - name: Start and verify PostgreSQL (Integration)
        run: |
          # CI FIX: Use database setup script for consistent test environment
          # Integration Tests: Ensure test databases and users are properly configured
          echo "Setting up PostgreSQL TEST environment..."

          # AGGRESSIVE CLEANUP: Clear ALL Docker Compose state for postgresql-test
          # Root cause: Persistent Compose state survives across workflow runs on self-hosted runners
          echo "Performing aggressive Docker Compose state cleanup for PostgreSQL..."

          # Step 1: Try to stop service gracefully (may fail if state is corrupted)
          docker compose stop postgresql-test 2>/dev/null || true

          # Step 2: Remove service with force and orphan cleanup
          docker compose rm --force --stop postgresql-test 2>/dev/null || true

          # Step 3: Down with all flags to clear all state
          docker compose down --remove-orphans --volumes postgresql-test 2>/dev/null || true

          # Step 4: Direct container removal as fallback
          docker rm -f raglite-postgresql-test 2>/dev/null || true

          # Step 5: CRITICAL - Clear any stale container references from Docker's state
          # This removes containers that Compose thinks exist but don't
          docker container prune -f 2>/dev/null || true

          # Step 6: Clear any dangling volumes that might hold stale references
          docker volume prune -f 2>/dev/null || true

          # Step 7: Kill any processes that might be holding port 5433
          if command -v lsof > /dev/null 2>&1; then
            lsof -ti:5433 | xargs -r kill -9 2>/dev/null || true
          fi

          # Step 8: Wait for Docker networking to fully release
          sleep 5

          # Start test services using docker-compose for consistency
          docker compose up -d postgresql-test

          # CRITICAL FIX: Wait for PostgreSQL to FULLY initialize (2 "ready" messages)
          # PostgreSQL emits "ready to accept connections" TWICE during full initialization:
          #   1st: After recovering WAL (NOT ready for queries yet)
          #   2nd: After completing startup sequence (fully ready)
          # Using docker logs to detect BOTH ready messages
          echo "Waiting for PostgreSQL to fully initialize..."
          MAX_INIT_WAIT=90  # Increased from 60s
          INIT_COUNT=0

          while [ $INIT_COUNT -lt $MAX_INIT_WAIT ]; do
            # Count occurrences of "ready" message (need 2 for full init)
            READY_COUNT=$(docker logs raglite-postgresql-test 2>&1 | grep -c "database system is ready to accept connections" || echo "0")

            if [ "$READY_COUNT" -ge 2 ]; then
              echo "✅ PostgreSQL fully initialized (${INIT_COUNT}s, ${READY_COUNT} ready messages)"
              sleep 3  # Extra buffer after full init
              break
            fi

            INIT_COUNT=$((INIT_COUNT + 1))
            if [ $((INIT_COUNT % 10)) -eq 0 ]; then
              echo "⏳ Waiting for full initialization... (${INIT_COUNT}s/${MAX_INIT_WAIT}s, ${READY_COUNT}/2 ready)"
            fi
            sleep 1
          done

          if [ $INIT_COUNT -eq $MAX_INIT_WAIT ]; then
            echo "❌ PostgreSQL did not fully initialize after ${MAX_INIT_WAIT}s"
            docker logs raglite-postgresql-test --tail 50
            exit 1
          fi

          # Run database setup script to create users/databases
          ./scripts/ci-database-setup.sh

          echo "✅ PostgreSQL TEST environment ready with raglite_ci database"

      - name: Initialize Qdrant collection
        # NOTE: Uses job-level env vars (APP_ENV=test, QDRANT_PORT=6335)
        # This ensures collection is created on the TEST Qdrant instance
        run: |
          source ci-venv/bin/activate
          echo "🔧 Initializing Qdrant collection (APP_ENV=${APP_ENV}, port ${QDRANT_PORT})..."
          python scripts/init-qdrant.py
          echo "✅ Qdrant collection initialized on port ${QDRANT_PORT}"

      - name: Initialize PostgreSQL schema
        # NOTE: Uses job-level env vars (APP_ENV=test, POSTGRES_PORT=5433, POSTGRES_DB=raglite_ci)
        run: |
          source ci-venv/bin/activate
          echo "🔧 Initializing PostgreSQL schema (${POSTGRES_DB} on port ${POSTGRES_PORT})..."
          python scripts/init-test-postgresql.py
          echo "✅ Schema initialized successfully"

      - name: Memory diagnostics (pre-test baseline)
        run: |
          echo "========================================="
          echo "Memory Diagnostics (Pre-Test Baseline)"
          echo "========================================="

          # System memory info (macOS)
          echo "📊 System Memory:"
          if command -v vm_stat &> /dev/null; then
            # macOS memory stats
            PAGE_SIZE=$(sysctl -n hw.pagesize 2>/dev/null || echo 4096)
            FREE_PAGES=$(vm_stat | grep "Pages free" | awk '{print $3}' | tr -d '.')
            INACTIVE_PAGES=$(vm_stat | grep "Pages inactive" | awk '{print $3}' | tr -d '.')
            WIRED_PAGES=$(vm_stat | grep "Pages wired down" | awk '{print $4}' | tr -d '.')
            ACTIVE_PAGES=$(vm_stat | grep "Pages active" | awk '{print $3}' | tr -d '.')
            SPECULATIVE_PAGES=$(vm_stat | grep "Pages speculative" | awk '{print $3}' | tr -d '.')

            FREE_MB=$((FREE_PAGES * PAGE_SIZE / 1024 / 1024))
            INACTIVE_MB=$((INACTIVE_PAGES * PAGE_SIZE / 1024 / 1024))
            WIRED_MB=$((WIRED_PAGES * PAGE_SIZE / 1024 / 1024))
            ACTIVE_MB=$((ACTIVE_PAGES * PAGE_SIZE / 1024 / 1024))
            SPECULATIVE_MB=$((SPECULATIVE_PAGES * PAGE_SIZE / 1024 / 1024))
            AVAILABLE_MB=$((FREE_MB + INACTIVE_MB + SPECULATIVE_MB))

            TOTAL_MEM=$(sysctl -n hw.memsize 2>/dev/null || echo 0)
            TOTAL_GB=$((TOTAL_MEM / 1024 / 1024 / 1024))

            echo "  Total RAM: ${TOTAL_GB} GB"
            echo "  Available: ${AVAILABLE_MB} MB (free: ${FREE_MB} MB + inactive: ${INACTIVE_MB} MB)"
            echo "  Active: ${ACTIVE_MB} MB"
            echo "  Wired: ${WIRED_MB} MB"

            # OOM risk assessment
            if [ $AVAILABLE_MB -lt 1024 ]; then
              echo "  ⚠️  WARNING: Low available memory (${AVAILABLE_MB}MB < 1GB)"
              echo "  ⚠️  Risk of OOM during tests - consider reducing parallelism"
            elif [ $AVAILABLE_MB -lt 2048 ]; then
              echo "  ⚠️  CAUTION: Memory available (${AVAILABLE_MB}MB) is moderate"
            else
              echo "  ✅ Memory looks healthy (${AVAILABLE_MB}MB available)"
            fi
          else
            # Fallback for Linux
            free -h 2>/dev/null || echo "Memory stats not available"
          fi

          echo ""
          echo "📊 Docker Container Memory:"
          docker stats --no-stream --format "table {{.Name}}\t{{.MemUsage}}\t{{.MemPerc}}" 2>/dev/null | head -10 || echo "No containers running"

          echo ""
          echo "📊 Top Memory Processes:"
          ps aux --sort=-%mem 2>/dev/null | head -6 || ps -eo pid,comm,%mem --sort=-%mem 2>/dev/null | head -6 || echo "Process stats not available"

          echo ""
          echo "========================================="

      - name: Validate integration test isolation markers
        run: |
          echo "========================================="
          echo "Validating integration test isolation markers"
          echo "========================================="
          python3 -c "
          from pathlib import Path
          import sys

          errors = []
          integration_dir = Path('tests/integration')

          for test_file in integration_dir.glob('test_*.py'):
              content = test_file.read_text()

              # Skip if not an integration test file (check for pytest.mark.integration)
              if 'pytest.mark.integration' not in content:
                  continue

              # Check for isolation markers in pytestmark (module level)
              has_preserve = 'preserve_collection' in content
              has_manages = 'manages_collection_state' in content

              # Must have at least one isolation marker in the file
              if not (has_preserve or has_manages):
                  errors.append(f'{test_file}: Missing isolation marker (preserve_collection or manages_collection_state)')

          if errors:
              print('❌ Integration test files missing isolation markers:')
              for err in errors:
                  print(f'  - {err}')
              print()
              print('Fix: Add pytest.mark.preserve_collection or pytest.mark.manages_collection_state to pytestmark')
              sys.exit(1)
          else:
              count = len(list(integration_dir.glob('test_*.py')))
              print(f'✅ All {count} integration test files have proper isolation markers')
          "

      - name: Run integration tests (CI fast mode)
        # NOTE: Environment variables are now set at job level (see test-integration.env)
        # This ensures ALL steps use consistent database configuration
        run: |
          source ci-venv/bin/activate

          # FIX: Add project root and scripts to PYTHONPATH
          # Integration tests also import from scripts directory
          export PYTHONPATH="${PWD}:${PWD}/scripts:${PYTHONPATH}"

          # Start background memory monitor (logs every 30s, captures OOM patterns)
          echo "Starting background memory monitor..."
          (
            while true; do
              TIMESTAMP=$(date +"%H:%M:%S")
              if command -v vm_stat &> /dev/null; then
                # macOS memory monitoring
                PAGE_SIZE=$(sysctl -n hw.pagesize 2>/dev/null || echo 4096)
                FREE_PAGES=$(vm_stat | grep "Pages free" | awk '{print $3}' | tr -d '.')
                INACTIVE_PAGES=$(vm_stat | grep "Pages inactive" | awk '{print $3}' | tr -d '.')
                WIRED_PAGES=$(vm_stat | grep "Pages wired down" | awk '{print $4}' | tr -d '.')
                ACTIVE_PAGES=$(vm_stat | grep "Pages active" | awk '{print $3}' | tr -d '.')

                FREE_MB=$((FREE_PAGES * PAGE_SIZE / 1024 / 1024))
                INACTIVE_MB=$((INACTIVE_PAGES * PAGE_SIZE / 1024 / 1024))
                AVAILABLE_MB=$((FREE_MB + INACTIVE_MB))
                ACTIVE_MB=$((ACTIVE_PAGES * PAGE_SIZE / 1024 / 1024))
                WIRED_MB=$((WIRED_PAGES * PAGE_SIZE / 1024 / 1024))

                echo "[$TIMESTAMP] MEM: available=${AVAILABLE_MB}MB active=${ACTIVE_MB}MB wired=${WIRED_MB}MB" >> memory_monitor.log

                # Alert if memory is critically low
                if [ $AVAILABLE_MB -lt 512 ]; then
                  echo "[$TIMESTAMP] ⚠️ CRITICAL: Only ${AVAILABLE_MB}MB available - OOM risk!" >> memory_monitor.log
                fi
              fi
              sleep 30
            done
          ) &
          MEMORY_MONITOR_PID=$!
          echo "Memory monitor PID: $MEMORY_MONITOR_PID"

          # Determine marker filter based on branch
          # Main branch: run ALL tests including slow (thorough validation before release)
          # Other branches: skip slow tests (faster feedback during development)
          if [ "$GITHUB_REF" == "refs/heads/main" ]; then
            MARKER_FILTER=""
            echo "========================================="
            echo "CI MODE: Full Testing on MAIN (APP_ENV=${APP_ENV})"
            echo "- Running ALL integration tests (including slow)"
          else
            MARKER_FILTER="not slow"
            echo "========================================="
            echo "CI MODE: Fast Testing (APP_ENV=${APP_ENV})"
            echo "- Running core integration tests (slow tests skipped)"
          fi
          echo "- Qdrant: ${QDRANT_HOST}:${QDRANT_PORT}"
          echo "- PostgreSQL: ${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}"
          echo "- Using 10-page sample PDF (~10-15s ingestion)"
          echo "- CRITICAL FIX: Sequential execution (-n 0) to prevent pytest-xdist worker controller errors"
          echo "- Target: 45-50 minutes total (sequential due to shared state)"
          echo "- PYTHONPATH: ${PYTHONPATH}"
          echo "========================================="

          TEST_EXIT_CODE=0
          if [ -d "tests/integration" ] && [ -n "$(find tests/integration -name 'test_*.py' -type f)" ]; then
            # CRITICAL FIX: Use sequential execution (-n 0) instead of parallel (-n 1)
            # pytest-xdist worker controller has internal errors with shared Qdrant collection state
            # Integration tests share session fixtures that don't work properly with worker processes
            # FIX: Added --capture=no for live output to prevent 13min runner communication timeout
            pytest tests/integration/ \
              -n 0 \
              -m "$MARKER_FILTER" \
              -v \
              --tb=short \
              --maxfail=3 \
              --capture=no \
              --enforce-isolation-markers \
              --junitxml=pytest-integration-report.xml || TEST_EXIT_CODE=$?
          else
            echo "⚠️  No integration tests found, skipping..."
          fi

          # Stop memory monitor
          kill $MEMORY_MONITOR_PID 2>/dev/null || true

          # Show memory log summary
          echo ""
          echo "========================================="
          echo "Memory Monitor Summary"
          echo "========================================="
          if [ -f memory_monitor.log ]; then
            echo "Memory samples during test run:"
            cat memory_monitor.log
            # Check for any critical warnings
            if grep -q "CRITICAL" memory_monitor.log; then
              echo ""
              echo "⚠️ CRITICAL memory warnings detected during test run!"
            fi
          else
            echo "No memory log generated"
          fi

          # Exit with test result
          exit $TEST_EXIT_CODE
        continue-on-error: false

      - name: Upload integration test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: integration-test-results
          path: pytest-integration-report.xml
          retention-days: 30

      - name: Cleanup containers (self-hosted runner hygiene)
        if: always()
        run: |
          echo "========================================="
          echo "Container Cleanup (Self-Hosted Runner)"
          echo "========================================="

          # AGGRESSIVE CLEANUP: Clear ALL Docker Compose state for test containers
          # Root cause: Persistent Compose state survives across workflow runs on self-hosted runners
          echo "Performing aggressive Docker Compose state cleanup for all test containers..."

          # Step 1: Try to stop services gracefully (may fail if state is corrupted)
          docker compose stop qdrant-test postgresql-test 2>/dev/null || true

          # Step 2: Remove services with force and orphan cleanup
          docker compose rm --force --stop qdrant-test postgresql-test 2>/dev/null || true

          # Step 3: Down with all flags to clear all state
          docker compose down --remove-orphans --volumes 2>/dev/null || true

          # Step 4: Direct container removal as fallback
          docker stop raglite-qdrant-test raglite-postgresql-test 2>/dev/null || true
          docker rm -f raglite-qdrant-test raglite-postgresql-test 2>/dev/null || true

          # Step 5: Clean up any orphaned containers from this workflow
          echo "Cleaning orphaned containers..."
          docker ps -a --filter "name=raglite-" --format '{{.Names}}' | xargs -r docker rm -f 2>/dev/null || true

          # Step 6: CRITICAL - Clear any stale container references from Docker's state
          # This removes containers that Compose thinks exist but don't
          echo "Pruning stale container references..."
          docker container prune -f 2>/dev/null || true

          # Step 7: Clear any dangling volumes that might hold stale references
          echo "Pruning dangling volumes..."
          docker volume prune -f 2>/dev/null || true

          # Step 8: Prune unused networks (prevents network exhaustion)
          echo "Pruning unused networks..."
          docker network prune -f 2>/dev/null || true

          # Report disk space (early warning for storage issues)
          echo ""
          echo "Runner disk space:"
          df -h / | tail -1

          echo ""
          echo "✅ Container cleanup complete"

  # ============================================================================
  # JOB 6: E2E Tests (~28 tests)
  # ============================================================================
  test-e2e:
    name: "🎯 Tests: E2E (~28 tests)"
    runs-on: [self-hosted, raglite]
    timeout-minutes: 20  # Increased from 15 to prevent runner communication timeout
    needs: [docker-health-check, test-unit]

    # FIX: Prevent parallel execution with resource-heavy jobs to avoid resource starvation
    concurrency:
      group: resource-heavy-tests-${{ github.ref }}
      cancel-in-progress: false

    env:
      APP_ENV: test
      POSTGRES_HOST: localhost
      POSTGRES_PORT: 5433
      POSTGRES_DB: raglite_ci
      POSTGRES_USER: raglite_ci
      POSTGRES_PASSWORD: raglite_ci
      CI: "true"

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install uv (network-resilient)
        run: |
          # Network-resilient uv installation with retry logic
          MAX_RETRIES=3
          RETRY_DELAY=2

          for attempt in $(seq 1 $MAX_RETRIES); do
            echo "Installing uv (attempt $attempt/$MAX_RETRIES)..."

            if curl --retry 3 --retry-delay 2 --retry-all-errors -LsSf https://astral.sh/uv/install.sh | sh; then
              echo "✅ uv installed successfully"

              # Verify installation
              if command -v uv &> /dev/null; then
                uv --version
                exit 0
              else
                echo "⚠️  uv command not found after installation"
              fi
            fi

            if [ $attempt -lt $MAX_RETRIES ]; then
              echo "⚠️  Installation attempt $attempt failed, retrying in ${RETRY_DELAY}s..."
              sleep $RETRY_DELAY
            fi
          done

          echo "❌ Failed to install uv after $MAX_RETRIES attempts"
          exit 1

      - name: Install dependencies with uv
        run: |
          uv venv ci-venv
          source ci-venv/bin/activate
          uv pip install -e .
          uv pip install \
            pytest==8.4.2 \
            pytest-asyncio==1.2.0 \
            "pytest-xdist>=3.5,<4.0" \
            "pytest-timeout>=2.0,<3.0" \
            pytest-mock>=3.15

      - name: Start and verify PostgreSQL (E2E)
        run: |
          # E2E Tests: Ensure PostgreSQL test environment is available
          echo "Setting up PostgreSQL TEST environment for E2E tests..."

          # CRITICAL FIX: Clean up corrupted data directory from cancelled jobs
          # Root cause: cancel-in-progress can leave pg_filenode.map in bad state
          if [ -d "./postgresql_data_test" ]; then
            echo "Checking PostgreSQL storage health..."
            # Check for corruption markers
            if [ ! -f "./postgresql_data_test/global/pg_filenode.map" ] || \
               find ./postgresql_data_test -name "*.lock" -type f 2>/dev/null | grep -q .; then
              echo "⚠️  Detected corrupted or locked PostgreSQL storage"
              echo "Clearing storage for fresh start..."
              rm -rf ./postgresql_data_test
            fi
          fi

          # AGGRESSIVE CLEANUP: Clear ALL Docker Compose state for postgresql-test
          # Root cause: Persistent Compose state survives across workflow runs on self-hosted runners
          echo "Performing aggressive Docker Compose state cleanup for PostgreSQL..."

          # Step 1: Try to stop service gracefully (may fail if state is corrupted)
          docker compose stop postgresql-test 2>/dev/null || true

          # Step 2: Remove service with force and orphan cleanup
          docker compose rm --force --stop postgresql-test 2>/dev/null || true

          # Step 3: Down with all flags to clear all state
          docker compose down --remove-orphans --volumes postgresql-test 2>/dev/null || true

          # Step 4: Direct container removal as fallback
          docker rm -f raglite-postgresql-test 2>/dev/null || true

          # Step 5: CRITICAL - Clear any stale container references from Docker's state
          # This removes containers that Compose thinks exist but don't
          docker container prune -f 2>/dev/null || true

          # Step 6: Clear any dangling volumes that might hold stale references
          docker volume prune -f 2>/dev/null || true

          # Step 7: Kill any processes holding port 5433
          if command -v lsof > /dev/null 2>&1; then
            lsof -ti:5433 | xargs -r kill -9 2>/dev/null || true
          fi

          # Step 8: Wait for Docker networking to fully release
          sleep 5

          # Start test services using docker-compose
          docker compose up -d postgresql-test

          # Wait for PostgreSQL to FULLY initialize (2 "ready" messages)
          echo "Waiting for PostgreSQL to fully initialize..."
          MAX_INIT_WAIT=90  # Increased from 60s
          INIT_COUNT=0

          while [ $INIT_COUNT -lt $MAX_INIT_WAIT ]; do
            # Count occurrences of "ready" message (need 2 for full init)
            READY_COUNT=$(docker logs raglite-postgresql-test 2>&1 | grep -c "database system is ready to accept connections" || echo "0")

            if [ "$READY_COUNT" -ge 2 ]; then
              echo "✅ PostgreSQL fully initialized (${INIT_COUNT}s, ${READY_COUNT} ready messages)"
              sleep 3  # Extra buffer after full init
              break
            fi

            INIT_COUNT=$((INIT_COUNT + 1))
            if [ $((INIT_COUNT % 10)) -eq 0 ]; then
              echo "⏳ Waiting for full initialization... (${INIT_COUNT}s/${MAX_INIT_WAIT}s, ${READY_COUNT}/2 ready)"
            fi
            sleep 1
          done

          if [ $INIT_COUNT -eq $MAX_INIT_WAIT ]; then
            echo "❌ PostgreSQL did not fully initialize after ${MAX_INIT_WAIT}s"
            echo "Container logs:"
            docker logs raglite-postgresql-test --tail 50
            exit 1
          fi

          # Run database setup script to create users/databases
          ./scripts/ci-database-setup.sh

          echo "✅ PostgreSQL TEST environment ready for E2E tests"

      - name: Initialize PostgreSQL schema (E2E)
        run: |
          source ci-venv/bin/activate
          echo "🔧 Initializing PostgreSQL schema for E2E tests..."
          python scripts/init-test-postgresql.py
          echo "✅ Schema initialized successfully"

      - name: Memory diagnostics (E2E pre-test)
        run: |
          echo "📊 E2E Pre-Test Memory Check:"
          if command -v vm_stat &> /dev/null; then
            PAGE_SIZE=$(sysctl -n hw.pagesize 2>/dev/null || echo 4096)
            FREE_PAGES=$(vm_stat | grep "Pages free" | awk '{print $3}' | tr -d '.')
            INACTIVE_PAGES=$(vm_stat | grep "Pages inactive" | awk '{print $3}' | tr -d '.')
            FREE_MB=$((FREE_PAGES * PAGE_SIZE / 1024 / 1024))
            INACTIVE_MB=$((INACTIVE_PAGES * PAGE_SIZE / 1024 / 1024))
            AVAILABLE_MB=$((FREE_MB + INACTIVE_MB))
            echo "  Available memory: ${AVAILABLE_MB}MB"
            if [ $AVAILABLE_MB -lt 1024 ]; then
              echo "  ⚠️ WARNING: Low memory (${AVAILABLE_MB}MB < 1GB)"
            else
              echo "  ✅ Memory adequate for E2E tests"
            fi
          fi
          echo ""
          docker stats --no-stream --format "table {{.Name}}\t{{.MemUsage}}" 2>/dev/null | head -5 || true

      - name: Run E2E tests
        run: |
          source ci-venv/bin/activate
          echo "========================================="
          echo "CI MODE: E2E Tests"
          echo "- Running ALL E2E tests (including slow)"
          echo "- Sequential execution (full isolation)"
          echo "- Target: <15 minutes"
          echo "========================================="
          if [ -d "tests/e2e" ] && [ -n "$(find tests/e2e -name 'test_*.py' -type f)" ]; then
            # FIX: Use --capture=no for live output to maintain runner connection
            pytest tests/e2e/ \
              -n 0 \
              -m "" \
              -v \
              --tb=line \
              --capture=no \
              --junitxml=pytest-e2e-report.xml
          else
            echo "⚠️  No E2E tests found, skipping..."
          fi

      - name: Upload E2E test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: e2e-test-results
          path: pytest-e2e-report.xml
          retention-days: 30

  # ============================================================================
  # NFR VALIDATION
  # ============================================================================
  # NOTE: NFR6/NFR7 Accuracy Validation runs in a separate workflow:
  # .github/workflows/accuracy-validation.yml
  #
  # This is a REQUIRED CHECK on PRs to main with independent timeout (60 min).
  # Runs in parallel with main CI (~5-10 min with cache).
  # Blocks PRs that would degrade accuracy below 70% threshold.
  # ============================================================================

  # ============================================================================
  # JOB 7: Performance Validation (NFR13)
  # ============================================================================
  test-performance:
    name: "⚡ NFR: Performance Validation (NFR13)"
    runs-on: [self-hosted, raglite]
    timeout-minutes: 20
    needs: [test-integration]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install uv (network-resilient)
        run: |
          # Network-resilient uv installation with retry logic
          MAX_RETRIES=3
          RETRY_DELAY=2

          for attempt in $(seq 1 $MAX_RETRIES); do
            echo "Installing uv (attempt $attempt/$MAX_RETRIES)..."

            if curl --retry 3 --retry-delay 2 --retry-all-errors -LsSf https://astral.sh/uv/install.sh | sh; then
              echo "✅ uv installed successfully"

              # Verify installation
              if command -v uv &> /dev/null; then
                uv --version
                exit 0
              else
                echo "⚠️  uv command not found after installation"
              fi
            fi

            if [ $attempt -lt $MAX_RETRIES ]; then
              echo "⚠️  Installation attempt $attempt failed, retrying in ${RETRY_DELAY}s..."
              sleep $RETRY_DELAY
            fi
          done

          echo "❌ Failed to install uv after $MAX_RETRIES attempts"
          exit 1

      - name: Install dependencies with uv
        run: |
          uv venv ci-venv
          source ci-venv/bin/activate
          uv pip install -e .
          uv pip install \
            pytest==8.4.2 \
            pytest-asyncio==1.2.0 \
            pytest-benchmark \
            pytest-mock>=3.15

      - name: Run performance tests
        env:
          QDRANT_HOST: localhost
          QDRANT_PORT: 6335
        run: |
          source ci-venv/bin/activate
          echo "========================================="
          echo "NFR13 Performance Validation"
          echo "- Target: <5s p50 query response"
          echo "- Target: <15s p95 query response"
          echo "- Sequential execution (accurate timing measurement)"
          echo "========================================="
          if [ -d "tests/performance" ] && [ -n "$(find tests/performance -name 'test_*.py' -type f)" ]; then
            pytest tests/performance/ \
              -n 0 \
              -v \
              --junitxml=pytest-performance-report.xml || true
          else
            echo "⚠️  No performance tests found, skipping..."
          fi
        continue-on-error: true

      - name: Upload performance test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: performance-validation-results
          path: pytest-performance-report.xml
          retention-days: 30

  # ============================================================================
  # JOB 7.5: Epic 6 Accuracy Gate (Story 6.7)
  # CI gate: Fail if MAPE exceeds 12%
  # ============================================================================
  test-epic6-accuracy:
    name: "📊 NFR: Epic 6 Accuracy Gate (MAPE <=12%)"
    runs-on: [self-hosted, raglite]
    timeout-minutes: 30
    needs: [docker-health-check, test-integration]
    env:
      APP_ENV: test
      QDRANT_HOST: localhost
      QDRANT_PORT: 6335
      POSTGRES_HOST: localhost
      POSTGRES_PORT: 5433
      POSTGRES_DB: raglite_ci
      POSTGRES_USER: raglite_ci
      POSTGRES_PASSWORD: raglite_ci
      CI: "true"

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install uv (network-resilient)
        run: |
          # Network-resilient uv installation with retry logic
          MAX_RETRIES=3
          RETRY_DELAY=2

          for attempt in $(seq 1 $MAX_RETRIES); do
            echo "Installing uv (attempt $attempt/$MAX_RETRIES)..."

            if curl --retry 3 --retry-delay 2 --retry-all-errors -LsSf https://astral.sh/uv/install.sh | sh; then
              echo "✅ uv installed successfully"

              # Verify installation
              if command -v uv &> /dev/null; then
                uv --version
                exit 0
              else
                echo "⚠️  uv command not found after installation"
              fi
            fi

            if [ $attempt -lt $MAX_RETRIES ]; then
              echo "⚠️  Installation attempt $attempt failed, retrying in ${RETRY_DELAY}s..."
              sleep $RETRY_DELAY
            fi
          done

          echo "❌ Failed to install uv after $MAX_RETRIES attempts"
          exit 1

      - name: Install dependencies with uv
        run: |
          uv venv ci-venv
          source ci-venv/bin/activate
          uv pip install -e .
          uv pip install \
            pytest==8.4.2 \
            pytest-asyncio==1.2.0 \
            "pytest-xdist>=3.5,<4.0" \
            "pytest-timeout>=2.0,<3.0" \
            pytest-mock>=3.15

      - name: Verify Qdrant is available
        run: |
          echo "Checking Qdrant test instance..."
          MAX_RETRIES=30
          RETRY_COUNT=0
          while [ $RETRY_COUNT -lt $MAX_RETRIES ]; do
            if curl -sf http://localhost:${QDRANT_PORT}/healthz > /dev/null 2>&1; then
              echo "✅ Qdrant is available at localhost:${QDRANT_PORT}"
              exit 0
            fi
            RETRY_COUNT=$((RETRY_COUNT + 1))
            echo "Waiting for Qdrant... (attempt $RETRY_COUNT/$MAX_RETRIES)"
            sleep 1
          done
          echo "❌ Qdrant not available after ${MAX_RETRIES}s"
          exit 1

      - name: Verify PostgreSQL is available
        run: |
          CONTAINER_NAME="raglite-postgresql-test"
          echo "Checking PostgreSQL instance ($CONTAINER_NAME)..."
          MAX_RETRIES=30
          RETRY_COUNT=0
          while [ $RETRY_COUNT -lt $MAX_RETRIES ]; do
            if docker exec $CONTAINER_NAME psql -U raglite_ci -d raglite_ci -c "SELECT 1" > /dev/null 2>&1; then
              echo "✅ PostgreSQL is available"
              exit 0
            fi
            RETRY_COUNT=$((RETRY_COUNT + 1))
            echo "Waiting for PostgreSQL... (attempt $RETRY_COUNT/$MAX_RETRIES)"
            sleep 1
          done
          echo "❌ PostgreSQL not available after ${MAX_RETRIES}s"
          exit 1

      - name: Run Epic 6 accuracy regression tests
        run: |
          source ci-venv/bin/activate

          export PYTHONPATH="${PWD}:${PWD}/scripts:${PYTHONPATH}"

          echo "========================================="
          echo "Epic 6 Accuracy Gate (Story 6.7)"
          echo "========================================="
          echo "- Target: MAPE <= 12%"
          echo "- Decision Gate:"
          echo "  - MAPE <= 10%: APPROVED"
          echo "  - MAPE 10-12%: WARNING (acceptable)"
          echo "  - MAPE > 12%: FAIL (trigger Story 6.8)"
          echo "- Ground Truth: tests/ground_truth/cement_demand_2020_2024.csv"
          echo "========================================="

          pytest tests/integration/test_epic6_accuracy_regression.py \
            -v \
            --junitxml=pytest-epic6-accuracy-report.xml
        continue-on-error: false  # CRITICAL: Fail CI if accuracy degrades

      - name: Upload Epic 6 accuracy test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: epic6-accuracy-results
          path: pytest-epic6-accuracy-report.xml
          retention-days: 30

  # ============================================================================
  # JOB 8: Agentic Workflow Test Suite (Story 3.8)
  # ============================================================================
  test-agentic-workflows:
    name: "🤖 Tests: Agentic Workflow Suite (Story 3.8)"
    runs-on: [self-hosted, raglite]
    timeout-minutes: 150  # Increased from 120 to prevent runner communication timeout (30min buffer)
    # OPTIMIZED: Runs in parallel with test-integration (not blocked by it)
    # Has own PostgreSQL container (port 5438) to avoid dependency on integration setup
    needs: [docker-health-check, test-unit]

    # FIX: Share concurrency group with E2E to prevent resource competition
    concurrency:
      group: resource-heavy-tests-${{ github.ref }}
      cancel-in-progress: false

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install uv (network-resilient)
        run: |
          # Network-resilient uv installation with retry logic
          MAX_RETRIES=3
          RETRY_DELAY=2

          for attempt in $(seq 1 $MAX_RETRIES); do
            echo "Installing uv (attempt $attempt/$MAX_RETRIES)..."

            if curl --retry 3 --retry-delay 2 --retry-all-errors -LsSf https://astral.sh/uv/install.sh | sh; then
              echo "✅ uv installed successfully"

              # Verify installation
              if command -v uv &> /dev/null; then
                uv --version
                exit 0
              else
                echo "⚠️  uv command not found after installation"
              fi
            fi

            if [ $attempt -lt $MAX_RETRIES ]; then
              echo "⚠️  Installation attempt $attempt failed, retrying in ${RETRY_DELAY}s..."
              sleep $RETRY_DELAY
            fi
          done

          echo "❌ Failed to install uv after $MAX_RETRIES attempts"
          exit 1

      - name: Install dependencies with uv
        run: |
          uv venv ci-venv
          source ci-venv/bin/activate
          uv pip install -e .
          uv pip install \
            pytest==8.4.2 \
            pytest-asyncio==1.2.0 \
            "pytest-xdist>=3.5,<4.0" \
            "pytest-timeout>=2.0,<3.0" \
            pytest-mock>=3.15 \
            pytest-json-report \
            numpy

      - name: Verify Qdrant is available
        run: |
          echo "Checking persistent Qdrant instance..."
          MAX_RETRIES=30
          RETRY_COUNT=0
          while [ $RETRY_COUNT -lt $MAX_RETRIES ]; do
            if curl -sf http://localhost:6333/healthz > /dev/null 2>&1; then
              echo "✅ Qdrant is available at localhost:6333"
              exit 0
            fi
            RETRY_COUNT=$((RETRY_COUNT + 1))
            echo "Waiting for Qdrant... (attempt $RETRY_COUNT/$MAX_RETRIES)"
            sleep 1
          done
          echo "❌ Qdrant not available after ${MAX_RETRIES}s"
          exit 1

      - name: Start and verify PostgreSQL (Agentic)
        run: |
          # PARALLEL OPTIMIZATION: Own container to run independently of test-integration
          # Uses unique port 5438 to avoid conflicts with other jobs
          CONTAINER_NAME="raglite-postgresql-agentic"
          CONTAINER_PORT="5438"

          echo "Ensuring PostgreSQL container '$CONTAINER_NAME' on port $CONTAINER_PORT..."

          # CRITICAL FIX: More aggressive cleanup to prevent port conflicts in CI
          # Remove ANY container using this port, not just named containers
          echo "Aggressively cleaning containers on port $CONTAINER_PORT..."
          docker ps -a --format '{{.Names}} {{.Ports}}' | grep ":${CONTAINER_PORT}->" | awk '{print $1}' | xargs -r docker rm -f || true
          sleep 3  # Increased wait time for Docker networking cleanup

          # Also kill any processes that might be holding the port
          if command -v lsof > /dev/null 2>&1; then
            lsof -ti:$CONTAINER_PORT | xargs -r kill -9 2>/dev/null || true
          fi

          # Check if container exists with correct credentials
          if docker ps -a --format '{{.Names}}' | grep -q "^${CONTAINER_NAME}$"; then
            if docker exec $CONTAINER_NAME psql -U raglite_ci -d postgres -c "SELECT 1" > /dev/null 2>&1; then
              echo "✅ Reusing existing container with correct credentials"
              if ! docker ps --format '{{.Names}}' | grep -q "^${CONTAINER_NAME}$"; then
                docker start $CONTAINER_NAME
                sleep 5
              fi
            else
              echo "Recreating container with correct credentials and resource limits..."
              docker rm -f $CONTAINER_NAME || true
              sleep 3
              # FIX: Add resource limits to prevent container resource starvation
              docker run -d \
                -p ${CONTAINER_PORT}:5432 \
                -e POSTGRES_DB=raglite_ci \
                -e POSTGRES_USER=raglite_ci \
                -e POSTGRES_PASSWORD=raglite_ci \
                --memory="2g" \
                --cpus="2.0" \
                --name $CONTAINER_NAME \
                postgres:16
            fi
          else
            echo "Creating new PostgreSQL container with resource limits..."
            # FIX: Add resource limits to prevent container resource starvation
            docker run -d \
              -p ${CONTAINER_PORT}:5432 \
              -e POSTGRES_DB=raglite_ci \
              -e POSTGRES_USER=raglite_ci \
              -e POSTGRES_PASSWORD=raglite_ci \
              --memory="2g" \
              --cpus="2.0" \
              --name $CONTAINER_NAME \
              postgres:16
          fi

          # CRITICAL FIX: Wait for PostgreSQL to FULLY initialize (2 "ready" messages)
          # PostgreSQL emits "ready to accept connections" TWICE during full initialization
          echo "Waiting for PostgreSQL initialization..."
          MAX_INIT_WAIT=90  # Increased from 60s
          INIT_COUNT=0

          while [ $INIT_COUNT -lt $MAX_INIT_WAIT ]; do
            # Count occurrences of "ready" message (need 2 for full init)
            READY_COUNT=$(docker logs $CONTAINER_NAME 2>&1 | grep -c "database system is ready to accept connections" || echo "0")

            if [ "$READY_COUNT" -ge 2 ]; then
              echo "✅ PostgreSQL fully initialized (${INIT_COUNT}s, ${READY_COUNT} ready messages)"
              sleep 3  # Extra buffer after full init
              break
            fi

            INIT_COUNT=$((INIT_COUNT + 1))
            if [ $((INIT_COUNT % 10)) -eq 0 ]; then
              echo "⏳ Waiting for full initialization... (${INIT_COUNT}s/${MAX_INIT_WAIT}s, ${READY_COUNT}/2 ready)"
            fi
            sleep 1
          done

          if [ $INIT_COUNT -eq $MAX_INIT_WAIT ]; then
            echo "❌ PostgreSQL did not initialize after ${MAX_INIT_WAIT}s"
            docker logs $CONTAINER_NAME --tail 50
            exit 1
          fi

          # Wait for PostgreSQL to be ready with enhanced error handling
          MAX_RETRIES=30  # Reduced since we already waited for initialization
          RETRY_COUNT=0
          while [ $RETRY_COUNT -lt $MAX_RETRIES ]; do
            if docker exec $CONTAINER_NAME psql -U raglite_ci -d raglite_ci -c "SELECT 1" > /dev/null 2>&1; then
              echo "✅ PostgreSQL is available at localhost:$CONTAINER_PORT"
              exit 0
            fi
            RETRY_COUNT=$((RETRY_COUNT + 1))
            if [ $((RETRY_COUNT % 5)) -eq 0 ]; then
              echo "Waiting for PostgreSQL... (attempt $RETRY_COUNT/$MAX_RETRIES)"
            fi
            sleep 1
          done
          echo "❌ PostgreSQL not available after ${MAX_RETRIES}s"
          echo "Container logs for debugging:"
          docker logs $CONTAINER_NAME --tail 20 || true
          exit 1

      - name: Run agentic workflow test suite
        env:
          QDRANT_HOST: localhost
          QDRANT_PORT: 6333  # Uses persistent production Qdrant
          POSTGRES_HOST: localhost
          POSTGRES_PORT: 5438  # OPTIMIZED: Own container (not shared with integration)
          POSTGRES_DB: raglite_ci
          POSTGRES_USER: raglite_ci
          POSTGRES_PASSWORD: raglite_ci
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          CI: "true"
        run: |
          source ci-venv/bin/activate

          export PYTHONPATH="${PWD}:${PWD}/scripts:${PYTHONPATH}"

          echo "========================================="
          echo "Agentic Workflow Test Suite (Story 3.8)"
          echo "========================================="
          echo "- Test Queries: 22 (17 analytical + 5 edge cases)"
          echo "- Success Rate Target: 80%+"
          echo "- Performance: p50 <12s, p95 <20s"
          echo "- Test Set: tests/fixtures/agentic_workflow_test_set.json"
          echo "========================================="

          pytest tests/integration/test_agentic_workflow_suite.py \
            -n 0 \
            -m "slow" \
            -v \
            --json-report \
            --json-report-file=test-reports/agentic_workflow_results.json \
            --junitxml=test-reports/agentic_workflow_junit.xml
        continue-on-error: true  # Don't block CI on test failures

      - name: Generate failure analysis report
        if: always()
        run: |
          source ci-venv/bin/activate

          echo "Generating failure analysis report..."
          if [ -f "test-reports/agentic_workflow_results.json" ]; then
            python scripts/generate_failure_report.py \
              test-reports/agentic_workflow_results.json \
              test-reports/agentic_workflow_failures.json || true
          else
            echo "⚠️  No test results found, skipping failure report"
          fi

      - name: Upload agentic workflow test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: agentic-workflow-test-results
          path: |
            test-reports/agentic_workflow_results.json
            test-reports/agentic_workflow_failures.json
            test-reports/agentic_workflow_junit.xml
          retention-days: 30

      - name: Display test summary
        if: always()
        run: |
          if [ -f "test-reports/agentic_workflow_failures.json" ]; then
            echo "========================================="
            echo "📊 AGENTIC WORKFLOW TEST SUMMARY"
            echo "========================================="
            cat test-reports/agentic_workflow_failures.json | jq -r '
              "Total Queries: \(.total_queries)",
              "Successes: \(.successes)",
              "Failures: \(.failures)",
              "Success Rate: \(.success_rate * 100 | round)%",
              "",
              "Top Recommendation:",
              "  \(.summary.recommendation[:100])..."
            '
            echo "========================================="
          fi

  # ============================================================================
  # JOB 9: Test Coverage Enforcement
  # OPTIMIZED: Uses cached coverage from test-unit (no redundant test execution)
  # ============================================================================
  test-coverage-enforcement:
    name: "📊 Coverage Gates (AC3-AC5)"
    runs-on: [self-hosted, raglite]
    timeout-minutes: 5  # Reduced from 15 - no test execution needed
    needs: [test-unit, test-integration]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Full history for git diff

      - name: Download unit test coverage artifact
        uses: actions/download-artifact@v4
        with:
          name: coverage-unit
          path: .

      - name: Install uv (network-resilient)
        run: |
          # Network-resilient uv installation with retry logic
          MAX_RETRIES=3
          RETRY_DELAY=2

          for attempt in $(seq 1 $MAX_RETRIES); do
            echo "Installing uv (attempt $attempt/$MAX_RETRIES)..."

            if curl --retry 3 --retry-delay 2 --retry-all-errors -LsSf https://astral.sh/uv/install.sh | sh; then
              echo "✅ uv installed successfully"

              # Verify installation
              if command -v uv &> /dev/null; then
                uv --version
                exit 0
              else
                echo "⚠️  uv command not found after installation"
              fi
            fi

            if [ $attempt -lt $MAX_RETRIES ]; then
              echo "⚠️  Installation attempt $attempt failed, retrying in ${RETRY_DELAY}s..."
              sleep $RETRY_DELAY
            fi
          done

          echo "❌ Failed to install uv after $MAX_RETRIES attempts"
          exit 1

      - name: Install coverage tools
        run: |
          set -e
          set -o pipefail

          uv venv ci-venv
          source ci-venv/bin/activate

          echo "========================================="
          echo "Installing coverage analysis tools..."
          echo "========================================="

          uv pip install coverage==7.6.1

      - name: Convert coverage.xml to JSON format
        run: |
          source ci-venv/bin/activate

          echo "========================================="
          echo "Using cached coverage from test-unit job"
          echo "========================================="

          # Verify coverage artifact was downloaded
          if [ -f "coverage.xml" ]; then
            echo "✅ Found coverage.xml from test-unit job"
            ls -la coverage.xml
          else
            echo "❌ coverage.xml not found!"
            exit 1
          fi

          # Convert XML to JSON for diff/ratchet scripts
          python3 scripts/convert_coverage_xml_to_json.py coverage.xml .coverage.json

      - name: Download baseline coverage from main
        id: download-baseline
        continue-on-error: true
        uses: dawidd6/action-download-artifact@v3
        with:
          workflow: ci.yml
          branch: main
          name: coverage-baseline
          path: .
          if_no_artifact_found: warn

      - name: Check coverage diff (AC3 - new code ≥80%)
        run: |
          source ci-venv/bin/activate

          echo "========================================="
          echo "AC3: Coverage Gate (New Code ≥80%)"
          echo "========================================="

          if [ -f ".coverage-baseline.json" ]; then
            python scripts/check_coverage_diff.py \
              --current=.coverage.json \
              --baseline=.coverage-baseline.json \
              --threshold=80
          else
            echo "⚠️  No baseline coverage found - skipping diff check"
            echo "   This is expected for first PR or main branch builds"
            python scripts/check_coverage_diff.py \
              --current=.coverage.json \
              --threshold=80
          fi

      - name: Check coverage ratchet (AC4 - no regression)
        run: |
          source ci-venv/bin/activate

          echo "========================================="
          echo "AC4: Coverage Ratchet (No Regression)"
          echo "========================================="

          if [ -f ".coverage-baseline.json" ]; then
            python scripts/check_coverage_ratchet.py \
              --current=.coverage.json \
              --baseline=.coverage-baseline.json
          else
            echo "⚠️  No baseline coverage found - skipping ratchet check"
            echo "   This is expected for first PR or main branch builds"
          fi

      - name: Post coverage comment to PR (AC5)
        if: github.event_name == 'pull_request'
        env:
          GITHUB_TOKEN: ${{ github.token }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          GITHUB_REF: ${{ github.ref }}
        run: |
          source ci-venv/bin/activate

          echo "Posting coverage comment to PR..."
          if [ -f ".coverage-baseline.json" ]; then
            python scripts/post_coverage_comment.py .coverage.json --baseline .coverage-baseline.json
          else
            python scripts/post_coverage_comment.py .coverage.json
          fi

      - name: Upload coverage report (HTML)
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage-report-html
          path: htmlcov/
          retention-days: 30

      - name: Upload coverage baseline (for main branch)
        if: github.ref == 'refs/heads/main'
        uses: actions/upload-artifact@v4
        with:
          name: coverage-baseline
          path: .coverage.json
          retention-days: 90

      - name: Display coverage summary
        if: always()
        run: |
          source ci-venv/bin/activate

          echo "========================================="
          echo "📊 COVERAGE SUMMARY"
          echo "========================================="

          if [ -f ".coverage.json" ]; then
            python3 scripts/display_coverage_summary.py .coverage.json || echo "Failed to display coverage summary"
          fi

          echo "========================================="

  # ============================================================================
  # JOB 10: Test Count Validation
  # ============================================================================
  test-count-validation:
    name: "🔢 NFR: Test Discovery Validation"
    runs-on: [self-hosted, raglite]
    timeout-minutes: 5
    needs: [docker-health-check, test-unit]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install uv (network-resilient)
        run: |
          # Network-resilient uv installation with retry logic
          MAX_RETRIES=3
          RETRY_DELAY=2

          for attempt in $(seq 1 $MAX_RETRIES); do
            echo "Installing uv (attempt $attempt/$MAX_RETRIES)..."

            if curl --retry 3 --retry-delay 2 --retry-all-errors -LsSf https://astral.sh/uv/install.sh | sh; then
              echo "✅ uv installed successfully"

              # Verify installation
              if command -v uv &> /dev/null; then
                uv --version
                exit 0
              else
                echo "⚠️  uv command not found after installation"
              fi
            fi

            if [ $attempt -lt $MAX_RETRIES ]; then
              echo "⚠️  Installation attempt $attempt failed, retrying in ${RETRY_DELAY}s..."
              sleep $RETRY_DELAY
            fi
          done

          echo "❌ Failed to install uv after $MAX_RETRIES attempts"
          exit 1

      - name: Install dependencies with uv
        run: |
          # Enable strict error handling
          set -e
          set -o pipefail

          uv venv ci-venv
          source ci-venv/bin/activate

          echo "========================================="
          echo "Installing dependencies..."
          echo "========================================="

          # Install raglite package with all production dependencies
          # This installs all dependencies listed in pyproject.toml [project.dependencies]
          echo "Installing raglite package..."
          uv pip install -e . || {
            echo "❌ Failed to install raglite package"
            exit 1
          }

          # Install ALL test dependencies from dependency-groups
          # CRITICAL: pytest-asyncio, pytest-xdist, pytest-timeout required for test collection
          echo "Installing test dependencies..."
          uv pip install \
            pytest==8.4.2 \
            pytest-asyncio==1.2.0 \
            "pytest-xdist>=3.5,<4.0" \
            "pytest-timeout>=2.0,<3.0" \
            pytest-mock>=3.15 pytest-mock || {
            echo "❌ Failed to install test dependencies"
            exit 1
          }

          # Verify installation
          echo ""
          echo "========================================="
          echo "Verifying installation..."
          echo "========================================="
          pytest --version || {
            echo "❌ pytest not installed correctly"
            exit 1
          }

          python -c "import raglite; print(f'✅ RAGLite package: {raglite.__file__}')" || {
            echo "❌ raglite package import failed"
            exit 1
          }

          python -c "import mistralai; print('✅ mistralai installed')" || {
            echo "❌ mistralai not installed - test collection will fail"
            exit 1
          }

          python -c "import qdrant_client; print('✅ qdrant_client installed')" || {
            echo "❌ qdrant_client not installed"
            exit 1
          }

          echo "✅ All dependencies installed successfully"
          echo ""

      - name: Start Qdrant for test discovery
        run: |
          echo "Ensuring Qdrant is available for test collection..."

          # ENHANCED: More robust startup for self-hosted runners
          QDRANT_PORT=6333
          QDRANT_URL="http://localhost:${QDRANT_PORT}"

          # Check if Qdrant is already running and healthy
          if curl -sf ${QDRANT_URL}/healthz > /dev/null 2>&1; then
            echo "✅ Qdrant already running and healthy at localhost:${QDRANT_PORT}"
          else
            echo "Qdrant not responsive - starting container..."

            # Enhanced cleanup for self-hosted runners
            docker ps -a --filter "name=raglite-qdrant" --format '{{.Names}}' | xargs -r docker rm -f 2>/dev/null || true
            if command -v lsof > /dev/null 2>&1; then
              lsof -ti:${QDRANT_PORT} | xargs -r kill -9 2>/dev/null || true
            fi
            sleep 3

            # Start container
            docker run -d \
              -p ${QDRANT_PORT}:6333 \
              --name raglite-qdrant \
              qdrant/qdrant:v1.15.0

            # Enhanced health check with timeout
            echo "Waiting for Qdrant to be ready (max 30s)..."
            MAX_RETRIES=30
            RETRY_COUNT=0
            while [ $RETRY_COUNT -lt $MAX_RETRIES ]; do
              # Verify container is running
              if ! docker ps --filter "name=raglite-qdrant" --format '{{.Names}}' | grep -q "raglite-qdrant"; then
                echo "❌ Container stopped unexpectedly!"
                docker logs raglite-qdrant --tail 30 || true
                exit 1
              fi

              # Check health
              if curl -sf ${QDRANT_URL}/healthz > /dev/null 2>&1; then
                echo "✅ Qdrant is ready (${RETRY_COUNT}s)"
                break
              fi

              RETRY_COUNT=$((RETRY_COUNT + 1))
              [ $((RETRY_COUNT % 10)) -eq 0 ] && echo "Still waiting... (${RETRY_COUNT}s/${MAX_RETRIES}s)"
              sleep 1
            done

            if [ $RETRY_COUNT -eq $MAX_RETRIES ]; then
              echo "❌ Qdrant not ready after ${MAX_RETRIES}s"
              docker logs raglite-qdrant --tail 50 || true
              exit 1
            fi
          fi

      - name: Start PostgreSQL for test discovery
        run: |
          # ROOT CAUSE #3 FIX: Use unique container name per job to avoid parallel job race condition
          # Test Discovery: raglite-postgresql-discovery on port 5434
          CONTAINER_NAME="raglite-postgresql-discovery"
          CONTAINER_PORT="5434"

          echo "Ensuring PostgreSQL container '$CONTAINER_NAME' with correct credentials..."

          # CRITICAL FIX: More aggressive cleanup to prevent port conflicts in CI
          # Remove ANY container using this port, not just named containers
          echo "Aggressively cleaning containers on port $CONTAINER_PORT..."
          docker ps -a --format '{{.Names}} {{.Ports}}' | grep ":${CONTAINER_PORT}->" | awk '{print $1}' | xargs -r docker rm -f || true
          sleep 3  # Increased wait time for Docker networking cleanup

          # Also kill any processes that might be holding the port
          if command -v lsof > /dev/null 2>&1; then
            lsof -ti:$CONTAINER_PORT | xargs -r kill -9 2>/dev/null || true
          fi

          # Check if container exists
          if docker ps -a --format '{{.Names}}' | grep -q "^${CONTAINER_NAME}$"; then
            echo "PostgreSQL container exists - verifying credentials..."

            # Check if container has correct user (raglite_ci)
            if docker exec $CONTAINER_NAME psql -U raglite_ci -d postgres -c "SELECT 1" > /dev/null 2>&1; then
              echo "✅ Container has correct credentials (raglite_ci) - reusing existing container"

              # Ensure it's running
              if ! docker ps --format '{{.Names}}' | grep -q "^${CONTAINER_NAME}$"; then
                echo "Container stopped - starting it..."
                docker start $CONTAINER_NAME
                sleep 5
              fi
            else
              echo "⚠️  Container has wrong credentials - recreating with raglite_ci..."
              docker rm -f $CONTAINER_NAME
              docker volume rm ${CONTAINER_NAME}-data 2>/dev/null || true

              # Wait for Docker to release port binding
              echo "Waiting for Docker networking cleanup (port $CONTAINER_PORT)..."
              sleep 5

              docker run -d \
                -p ${CONTAINER_PORT}:5432 \
                -e POSTGRES_DB=raglite_ci \
                -e POSTGRES_USER=raglite_ci \
                -e POSTGRES_PASSWORD=raglite_ci \
                --name $CONTAINER_NAME \
                postgres:16
            fi
          else
            echo "Creating new PostgreSQL container..."
            docker run -d \
              -p ${CONTAINER_PORT}:5432 \
              -e POSTGRES_DB=raglite_ci \
              -e POSTGRES_USER=raglite_ci \
              -e POSTGRES_PASSWORD=raglite_ci \
              --name $CONTAINER_NAME \
              postgres:16
          fi

          # CRITICAL FIX: Wait for PostgreSQL to FULLY initialize (2 "ready" messages)
          # PostgreSQL emits "ready to accept connections" TWICE during full initialization
          echo "Waiting for PostgreSQL initialization..."
          MAX_INIT_WAIT=90  # Increased from 60s
          INIT_COUNT=0

          while [ $INIT_COUNT -lt $MAX_INIT_WAIT ]; do
            # Count occurrences of "ready" message (need 2 for full init)
            READY_COUNT=$(docker logs $CONTAINER_NAME 2>&1 | grep -c "database system is ready to accept connections" || echo "0")

            if [ "$READY_COUNT" -ge 2 ]; then
              echo "✅ PostgreSQL fully initialized (${INIT_COUNT}s, ${READY_COUNT} ready messages)"
              sleep 3  # Extra buffer after full init
              break
            fi

            INIT_COUNT=$((INIT_COUNT + 1))
            if [ $((INIT_COUNT % 10)) -eq 0 ]; then
              echo "⏳ Waiting for full initialization... (${INIT_COUNT}s/${MAX_INIT_WAIT}s, ${READY_COUNT}/2 ready)"
            fi
            sleep 1
          done

          if [ $INIT_COUNT -eq $MAX_INIT_WAIT ]; then
            echo "❌ PostgreSQL did not initialize after ${MAX_INIT_WAIT}s"
            docker logs $CONTAINER_NAME --tail 50
            exit 1
          fi

          echo "✅ PostgreSQL container ready"

      - name: Validate test count
        run: |
          source ci-venv/bin/activate
          echo "========================================="
          echo "Validating test discovery..."
          echo "========================================="

          # Enable strict error handling
          set -e
          set -o pipefail

          # Count total tests (including slow) - SHOW ERRORS for debugging
          # Updated 2025-12-18: Account for benchmark parametric tests (3000+ permutations)
          EXPECTED_MIN_TESTS=300
          EXPECTED_MAX_TESTS=5000
          echo "Collecting all tests (including slow)..."

          # Capture full output for debugging
          COLLECT_OUTPUT=$(python -m pytest --collect-only -q tests/ -m "" 2>&1 | tee /tmp/pytest-collect-total.log || true)
          TOTAL_TESTS=$(echo "$COLLECT_OUTPUT" | tail -1 | grep -oE '[0-9]+' | head -1 || echo "")

          # If collection failed or returned empty, show error and exit
          if [ -z "$TOTAL_TESTS" ]; then
            echo "❌ PYTEST TEST COLLECTION FAILED!"
            echo ""
            echo "Pytest output:"
            cat /tmp/pytest-collect-total.log
            echo ""
            echo "Debugging info:"
            echo "  Python version: $(python --version)"
            echo "  Pytest version: $(pytest --version)"
            echo "  Working directory: $(pwd)"
            echo "  Test directory exists: $([ -d tests ] && echo 'YES' || echo 'NO')"
            exit 1
          fi

          # Count fast tests (default - excludes slow)
          echo "Collecting fast tests (excluding slow)..."
          COLLECT_FAST=$(python -m pytest --collect-only -q tests/ 2>&1 | tee /tmp/pytest-collect-fast.log || true)
          FAST_TESTS=$(echo "$COLLECT_FAST" | tail -1 | grep -oE '[0-9]+' | head -1 || echo "0")

          # Count slow tests
          echo "Collecting slow tests..."
          COLLECT_SLOW=$(python -m pytest --collect-only -q tests/ -m "slow" 2>&1 | tee /tmp/pytest-collect-slow.log || true)
          SLOW_TESTS=$(echo "$COLLECT_SLOW" | tail -1 | grep -oE '[0-9]+' | head -1 || echo "0")

          echo ""
          echo "========================================="
          echo "Test Discovery Results:"
          echo "========================================="
          echo "Total tests (including slow): $TOTAL_TESTS"
          echo "Fast tests (default):         $FAST_TESTS"
          echo "Slow tests:                   $SLOW_TESTS"
          echo "Expected range:               $EXPECTED_MIN_TESTS - $EXPECTED_MAX_TESTS"
          echo ""

          # Validate total test count is within expected range
          if [ "$TOTAL_TESTS" -ge "$EXPECTED_MIN_TESTS" ] && [ "$TOTAL_TESTS" -le "$EXPECTED_MAX_TESTS" ]; then
            echo "✅ Test discovery validated ($TOTAL_TESTS tests, $SLOW_TESTS marked slow)"
          elif [ "$TOTAL_TESTS" -lt "$EXPECTED_MIN_TESTS" ]; then
            echo "❌ Test count too low ($TOTAL_TESTS < $EXPECTED_MIN_TESTS)"
            echo "⚠️  Some tests may not be discovered by pytest"
            echo ""
            echo "Debugging: Showing last 20 lines of collection output:"
            tail -20 /tmp/pytest-collect-total.log
            exit 1
          else
            echo "⚠️  Test count higher than expected ($TOTAL_TESTS > $EXPECTED_MAX_TESTS)"
            echo "This might indicate benchmark test parametric explosions"
            echo "Validation continues but consider reviewing benchmark test design"
          fi

  # ============================================================================
  # DOCUMENTATION & SUMMARY
  # ============================================================================

  # ============================================================================
  # JOB 11: Documentation Validation
  # ============================================================================
  docs-validation:
    name: "📚 Documentation: Validation"
    runs-on: [self-hosted, raglite]
    timeout-minutes: 5

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run comprehensive documentation validation
        run: |
          echo "Running comprehensive documentation validation..."
          # Use system python3 (consistent with other self-hosted jobs)
          # Avoids permission errors with actions/setup-python on self-hosted runners
          python3 scripts/validate-docs.py

  # ============================================================================
  # JOB 12: Burn-In Loop (Flaky Test Detection)
  # ============================================================================
  burn-in:
    name: "🔥 Quality Gate: Burn-In Loop (3 iterations)"
    runs-on: [self-hosted, raglite]
    timeout-minutes: 60
    # Only run on main branch post-merge (direct pushes to main)
    # Skip on: PRs to main, feature branches, develop branch
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    needs: [docker-health-check, test-unit, test-integration, test-e2e]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install uv (network-resilient)
        run: |
          # Network-resilient uv installation with retry logic
          MAX_RETRIES=3
          RETRY_DELAY=2

          for attempt in $(seq 1 $MAX_RETRIES); do
            echo "Installing uv (attempt $attempt/$MAX_RETRIES)..."

            if curl --retry 3 --retry-delay 2 --retry-all-errors -LsSf https://astral.sh/uv/install.sh | sh; then
              echo "✅ uv installed successfully"

              # Verify installation
              if command -v uv &> /dev/null; then
                uv --version
                exit 0
              else
                echo "⚠️  uv command not found after installation"
              fi
            fi

            if [ $attempt -lt $MAX_RETRIES ]; then
              echo "⚠️  Installation attempt $attempt failed, retrying in ${RETRY_DELAY}s..."
              sleep $RETRY_DELAY
            fi
          done

          echo "❌ Failed to install uv after $MAX_RETRIES attempts"
          exit 1

      - name: Install dependencies with uv
        run: |
          uv venv ci-venv
          source ci-venv/bin/activate
          uv pip install -e .
          uv pip install \
            pytest==8.4.2 \
            pytest-asyncio==1.2.0 \
            "pytest-xdist>=3.5,<4.0" \
            "pytest-timeout>=2.0,<3.0" \
            pytest-mock>=3.15

      - name: Verify Qdrant is available
        run: |
          echo "Checking persistent Qdrant instance..."
          MAX_RETRIES=30
          RETRY_COUNT=0
          while [ $RETRY_COUNT -lt $MAX_RETRIES ]; do
            if curl -sf http://localhost:6333/healthz > /dev/null 2>&1; then
              echo "✅ Qdrant is available at localhost:6333"
              exit 0
            fi
            RETRY_COUNT=$((RETRY_COUNT + 1))
            echo "Waiting for Qdrant... (attempt $RETRY_COUNT/$MAX_RETRIES)"
            sleep 1
          done
          echo "❌ Qdrant not available after ${MAX_RETRIES}s"
          exit 1

      - name: Start and verify PostgreSQL (Burn-in)
        run: |
          # ROOT CAUSE #3 FIX: Use unique container name per job to avoid parallel job race condition
          # Burn-in Tests: raglite-postgresql-burnin on port 5435
          CONTAINER_NAME="raglite-postgresql-burnin"
          CONTAINER_PORT="5435"

          echo "Ensuring PostgreSQL container '$CONTAINER_NAME' with correct credentials..."

          # CRITICAL FIX: More aggressive cleanup to prevent port conflicts in CI
          # Remove ANY container using this port, not just named containers
          echo "Aggressively cleaning containers on port $CONTAINER_PORT..."
          docker ps -a --format '{{.Names}} {{.Ports}}' | grep ":${CONTAINER_PORT}->" | awk '{print $1}' | xargs -r docker rm -f || true
          sleep 3  # Increased wait time for Docker networking cleanup

          # Also kill any processes that might be holding the port
          if command -v lsof > /dev/null 2>&1; then
            lsof -ti:$CONTAINER_PORT | xargs -r kill -9 2>/dev/null || true
          fi

          # Check if container exists
          if docker ps -a --format '{{.Names}}' | grep -q "^${CONTAINER_NAME}$"; then
            echo "PostgreSQL container exists - verifying credentials..."

            # Check if container has correct user (raglite_ci)
            if docker exec $CONTAINER_NAME psql -U raglite_ci -d postgres -c "SELECT 1" > /dev/null 2>&1; then
              echo "✅ Container has correct credentials (raglite_ci) - reusing existing container"

              # Ensure it's running
              if ! docker ps --format '{{.Names}}' | grep -q "^${CONTAINER_NAME}$"; then
                echo "Container stopped - starting it..."
                docker start $CONTAINER_NAME
                sleep 5
              fi
            else
              echo "⚠️  Container has wrong credentials - recreating with raglite_ci..."
              docker rm -f $CONTAINER_NAME
              docker volume rm ${CONTAINER_NAME}-data 2>/dev/null || true

              # Wait for Docker to release port binding
              echo "Waiting for Docker networking cleanup (port $CONTAINER_PORT)..."
              sleep 5

              docker run -d \
                -p ${CONTAINER_PORT}:5432 \
                -e POSTGRES_DB=raglite_ci \
                -e POSTGRES_USER=raglite_ci \
                -e POSTGRES_PASSWORD=raglite_ci \
                --name $CONTAINER_NAME \
                postgres:16
            fi
          else
            echo "Creating new PostgreSQL container..."
            docker run -d \
              -p ${CONTAINER_PORT}:5432 \
              -e POSTGRES_DB=raglite_ci \
              -e POSTGRES_USER=raglite_ci \
              -e POSTGRES_PASSWORD=raglite_ci \
              --name $CONTAINER_NAME \
              postgres:16
          fi

          # CRITICAL FIX: Wait for PostgreSQL to FULLY initialize (2 "ready" messages)
          # PostgreSQL emits "ready to accept connections" TWICE during full initialization
          echo "Waiting for PostgreSQL initialization..."
          MAX_INIT_WAIT=90  # Increased from 60s
          INIT_COUNT=0

          while [ $INIT_COUNT -lt $MAX_INIT_WAIT ]; do
            # Count occurrences of "ready" message (need 2 for full init)
            READY_COUNT=$(docker logs $CONTAINER_NAME 2>&1 | grep -c "database system is ready to accept connections" || echo "0")

            if [ "$READY_COUNT" -ge 2 ]; then
              echo "✅ PostgreSQL fully initialized (${INIT_COUNT}s, ${READY_COUNT} ready messages)"
              sleep 3  # Extra buffer after full init
              break
            fi

            INIT_COUNT=$((INIT_COUNT + 1))
            if [ $((INIT_COUNT % 10)) -eq 0 ]; then
              echo "⏳ Waiting for full initialization... (${INIT_COUNT}s/${MAX_INIT_WAIT}s, ${READY_COUNT}/2 ready)"
            fi
            sleep 1
          done

          if [ $INIT_COUNT -eq $MAX_INIT_WAIT ]; then
            echo "❌ PostgreSQL did not initialize after ${MAX_INIT_WAIT}s"
            docker logs $CONTAINER_NAME --tail 50
            exit 1
          fi

          echo "✅ PostgreSQL container ready"

      - name: Initialize Qdrant collection
        run: |
          source ci-venv/bin/activate
          echo "🔧 Initializing Qdrant collection (financial_docs)..."
          python scripts/init-qdrant.py
          echo "✅ Qdrant collection initialized"

      - name: Initialize PostgreSQL schema
        env:
          APP_ENV: test
          POSTGRES_HOST: localhost
          POSTGRES_PORT: 5435
          POSTGRES_DB: raglite_ci
          POSTGRES_USER: raglite_ci
          POSTGRES_PASSWORD: raglite_ci
        run: |
          source ci-venv/bin/activate
          echo "🔧 Initializing PostgreSQL schema (financial_chunks, financial_tables, entity_mappings)..."
          python scripts/init-test-postgresql.py
          echo "✅ Schema initialized successfully"

      - name: Run burn-in loop (3 iterations)
        env:
          QDRANT_HOST: localhost
          QDRANT_PORT: 6335
          POSTGRES_HOST: localhost
          POSTGRES_PORT: 5435  # Burn-in uses unique port to avoid race condition
          POSTGRES_DB: raglite_ci
          POSTGRES_USER: raglite_ci
          POSTGRES_PASSWORD: raglite_ci
          TEST_USE_FULL_PDF: "false"
          CI: "true"
        run: |
          source ci-venv/bin/activate

          # FIX: Add project root and scripts to PYTHONPATH
          # Burn-in runs full test suite which includes tests importing from scripts
          export PYTHONPATH="${PWD}:${PWD}/scripts:${PYTHONPATH}"

          echo "========================================="
          echo "🔥 BURN-IN LOOP: Flaky Test Detection"
          echo "========================================="
          echo "- Iterations: 3 (quick feedback)"
          echo "- Scope: Full test suite"
          echo "- Failure policy: ANY failure = FLAKY"
          echo "- Target: <20 minutes total"
          echo "- PYTHONPATH: ${PYTHONPATH}"
          echo "========================================="
          echo ""

          # Track failures across iterations
          FAILED_ITERATIONS=0

          for i in {1..3}; do
            echo ""
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            echo "🔥 Burn-in iteration $i/3"
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            echo ""

            # Run full suite (fast tests only - skip slow)
            # CRITICAL FIX: Use sequential execution to avoid pytest-xdist worker controller issues
            if pytest tests/ \
              -n 0 \
              -m "not slow" \
              -v \
              --junitxml=burn-in-iteration-$i.xml 2>&1 | tee burn-in-iteration-$i.log; then
              echo "✅ Iteration $i PASSED"
            else
              echo "❌ Iteration $i FAILED"
              FAILED_ITERATIONS=$((FAILED_ITERATIONS + 1))

              # Continue to run remaining iterations to collect all failures
              echo "⚠️  Continuing to iteration $((i + 1)) to collect failure patterns..."
            fi
            echo ""
          done

          echo ""
          echo "========================================="
          echo "🔥 BURN-IN RESULTS"
          echo "========================================="
          echo "Total iterations: 3"
          echo "Failed iterations: $FAILED_ITERATIONS"
          echo ""

          if [ $FAILED_ITERATIONS -gt 0 ]; then
            echo "❌ FLAKY TESTS DETECTED!"
            echo ""
            echo "⚠️  $FAILED_ITERATIONS out of 3 iterations failed."
            echo "This indicates non-deterministic test behavior."
            echo ""
            echo "Next steps:"
            echo "1. Review burn-in artifacts (logs + XML reports)"
            echo "2. Identify failing tests across iterations"
            echo "3. Fix flaky tests before merging"
            echo ""
            exit 1
          else
            echo "✅ ALL ITERATIONS PASSED"
            echo "No flaky tests detected - safe to merge!"
          fi

      - name: Upload burn-in artifacts on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: burn-in-failure-artifacts
          path: |
            burn-in-iteration-*.xml
            burn-in-iteration-*.log
          retention-days: 30

  # ============================================================================
  # JOB 13: Build Summary
  # ============================================================================
  build-summary:
    name: "📊 CI Pipeline: Summary"
    runs-on: [self-hosted, raglite]
    needs: [lint, file-size-check, type-check, security, test-unit, test-integration, test-e2e, test-performance, test-epic6-accuracy, test-agentic-workflows, test-coverage-enforcement, test-count-validation, docs-validation, burn-in]
    if: always()

    steps:
      - name: Check all jobs status
        run: |
          echo "============================================================"
          echo "📊 CI PIPELINE SUMMARY"
          echo "============================================================"
          echo ""
          echo "📝 CODE QUALITY:"
          echo "  Lint & Format:     ${{ needs.lint.result }}"
          echo "  File Size Limits:  ${{ needs.file-size-check.result }}"
          echo "  Type Check:        ${{ needs.type-check.result }}"
          echo "  Security Scan:     ${{ needs.security.result }}"
          echo ""
          echo "🧪 TEST SUITES (394 tests total):"
          echo "  Unit Tests:        ${{ needs.test-unit.result }} (~236 tests)"
          echo "  Integration:       ${{ needs.test-integration.result }} (~115 tests)"
          echo "  E2E Tests:         ${{ needs.test-e2e.result }} (~28 tests)"
          echo ""
          echo "🤖 AGENTIC WORKFLOW TESTS (Epic 3):"
          echo "  Workflow Suite:    ${{ needs.test-agentic-workflows.result }} (22 analytical queries)"
          echo ""
          echo "✅ NFR VALIDATION:"
          echo "  Performance (NFR13):   ${{ needs.test-performance.result }}"
          echo "  Epic 6 Accuracy:       ${{ needs.test-epic6-accuracy.result }} (MAPE <= 12%)"
          echo "  Test Discovery:        ${{ needs.test-count-validation.result }}"
          echo ""
          echo "  NOTE: NFR6/NFR7 (Accuracy) runs in PARALLEL as required PR check:"
          echo "        .github/workflows/accuracy-validation.yml (~5-10 min with cache)"
          echo ""
          echo "🔥 QUALITY GATES:"
          echo "  Burn-In Loop:      ${{ needs.burn-in.result }} (3 iterations)"
          echo ""
          echo "📚 DOCUMENTATION:"
          echo "  Validation:        ${{ needs.docs-validation.result }}"
          echo ""
          echo "============================================================"
          echo ""

          # Critical: Unit tests must pass
          if [[ "${{ needs.test-unit.result }}" == "failure" ]]; then
            echo "❌ Unit tests failed - blocking merge"
            exit 1
          fi

          # Critical: Linting must pass
          if [[ "${{ needs.lint.result }}" == "failure" ]]; then
            echo "❌ Linting failed - blocking merge"
            exit 1
          fi

          # Critical: Test discovery must pass
          if [[ "${{ needs.test-count-validation.result }}" == "failure" ]]; then
            echo "❌ Test discovery validation failed - blocking merge"
            echo "   Some tests may not be discovered by pytest!"
            exit 1
          fi

          # Critical: Documentation must exist
          if [[ "${{ needs.docs-validation.result }}" == "failure" ]]; then
            echo "❌ Documentation validation failed - blocking merge"
            exit 1
          fi

          # Critical: Epic 6 accuracy gate (Story 6.7)
          if [[ "${{ needs.test-epic6-accuracy.result }}" == "failure" ]]; then
            echo "❌ Epic 6 accuracy gate failed (MAPE > 12%) - blocking merge"
            echo "   Review test-epic6-accuracy job and consider triggering Story 6.8"
            exit 1
          fi

          # Warnings for non-critical failures
          if [[ "${{ needs.type-check.result }}" == "failure" ]]; then
            echo "⚠️  Type checking issues detected (non-blocking)"
          fi

          if [[ "${{ needs.security.result }}" == "failure" ]]; then
            echo "⚠️  Security scan issues detected (non-blocking)"
          fi

          if [[ "${{ needs.test-integration.result }}" == "failure" ]]; then
            echo "⚠️  Integration test issues detected (non-blocking)"
          fi

          if [[ "${{ needs.test-e2e.result }}" == "failure" ]]; then
            echo "⚠️  E2E test issues detected (non-blocking)"
          fi

          if [[ "${{ needs.test-performance.result }}" == "failure" ]]; then
            echo "⚠️  Performance test issues detected (non-blocking)"
            echo "   Review NFR13 compliance"
          fi

          if [[ "${{ needs.burn-in.result }}" == "failure" ]]; then
            echo "⚠️  Burn-in loop detected flaky tests (non-blocking)"
            echo "   Review burn-in artifacts to identify non-deterministic tests"
            echo "   Strongly recommended to fix before merging"
          fi

          echo ""
          echo "✅ All critical checks passed - ready to merge"
